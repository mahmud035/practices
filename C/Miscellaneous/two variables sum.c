#include <stdio.h>
#include <conio.h>
int main()
{
    int a, b, sum;

    printf("enter the first value:");
    scanf("%d", &a);

    printf("enter the second value:");
    scanf("%d", &b);

    sum = a + b;
    printf("sum of c is:%d", sum);

    return 0;
}
