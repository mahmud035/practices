#include <stdio.h>

int main()
{
  int num1;
  float f;
  char c;
  // scanf("%d %f %c", &num1, &f, &c);
  // printf("%d %0.2f %c", num1, f, c);

  int p, q;
  // scanf("%d%% %d%%", &p, &q);
  // printf("%d%% %d%%", p, q);

  int a, b;
  scanf("%da %db", &a, &b);
  printf("%da %db", a, b);

  return 0;
}