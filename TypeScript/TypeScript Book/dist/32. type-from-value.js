"use strict";
{
    //  Type from Value
    // Type from Value in TypeScript refers to the "automatic inference of a type from a value of expression through type inference".
    const x = 'x'; // type infer as 'x'
    let y = 'y'; // type infer as 'string'
}
