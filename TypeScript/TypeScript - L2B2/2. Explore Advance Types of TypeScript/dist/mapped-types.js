"use strict";
const arrayOfNumbers = [1, 2, 3];
const arrayOfStrings = arrayOfNumbers.map((number) => number.toString());
console.log(arrayOfStrings);
const rectangularArea = {
    width: 10,
    height: 12,
};
const name1 = { name: 'Persian' };
const area1 = {
    width: 10,
    height: 20,
};
const volume = {
    width: '10',
    height: 20,
    depth: '30',
};
