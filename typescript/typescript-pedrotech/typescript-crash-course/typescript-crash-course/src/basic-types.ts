const id: number = 3;
const companyName: string = 'Enosis';
const isPublished: boolean = true;

const ids: number[] = [1, 2, 3, 4, 5];
const names: string[] = ['Alex', 'John'];

type Status = 'Pending' | 'Success' | 'Failed';
